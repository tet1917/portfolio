import React from "react";
import styles from "./footer.module.css";
const { footer } = styles;

export const Footer = () => {
  return (
    <footer className={footer}>
      <p>© tetsu kazui</p>
    </footer>
  );
};
